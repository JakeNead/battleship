const receiveAttack = require("../src/gameboard");

test.skip("", () => {
  expect(receiveAttack([0, 0])).toBeFalsy();
});

test.skip("", () => {
  expect(receiveAttack([0, 0])).toBeFalsy();
});

test.skip("", () => {
  expect(receiveAttack([0, 0])).toBeFalsy();
});
